// Importing the Header component from the "Header" file
import Header from "./Header";

// Importing the Footer component from the "Footer" file
import Footer from "./Footer";

// Importing the Login component from the "LoginForm" file
import Login from "./LoginForm";

// Importing the Register component from the "RegisterForm" file
import Register from "./RegisterForm";

// Importing the UserComponents component from the "UserComponents" file
import UserComponents from "./UserComponents";

// Exporting the imported components for use in other parts of the application
export {
    Header,          // Exporting the Header component
    Footer,          // Exporting the Footer component
    Login,           // Exporting the Login component
    Register,        // Exporting the Register component
    UserComponents,  // Exporting the UserComponents component
}
